<Window x:Class="PlanYourLife1_4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlanYourLife1_4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="10">
        <DataGrid Name="DataGridPlanYourLife" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,24,0,0">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Date">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding date}" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Text" Binding="{Binding text}"/>
                <DataGridCheckBoxColumn Header="Check" Binding="{Binding implementation}"/>

            </DataGrid.Columns>

        </DataGrid>
        <Menu HorizontalAlignment="Left" Height="24" VerticalAlignment="Top"  >
            <MenuItem Header="Add" Click="MenuItemAdd_Click"/>
        </Menu>

    </Grid>
</Window>
